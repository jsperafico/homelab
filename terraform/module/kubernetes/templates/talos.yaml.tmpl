machine:
  install:
    disk: ${install_disk}
  network:
    interfaces:
      - deviceSelector:
          driver: virtio_net
        addresses:
          - ${ip_address}/24
        routes:
          - network: 0.0.0.0/0
            gateway: ${gateway}